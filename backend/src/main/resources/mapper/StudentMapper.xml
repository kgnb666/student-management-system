<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.score.mapper.StudentMapper">

    <select id="selectStudentList" resultType="com.example.score.vo.StudentVO">
        SELECT s.id,
               s.student_no,
               s.name,
               s.gender,
               s.birth_date,
               s.phone,
               s.class_id,
               c.class_name,
               s.status
        FROM student s
                 LEFT JOIN class_info c ON s.class_id = c.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (s.student_no LIKE CONCAT('%', #{keyword}, '%')
                OR s.name LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="classId != null">
                AND s.class_id = #{classId}
            </if>
        </where>
        ORDER BY s.student_no
    </select>

    <select id="selectStudentVO" resultType="com.example.score.vo.StudentVO">
        SELECT s.id,
               s.student_no,
               s.name,
               s.gender,
               s.birth_date,
               s.phone,
               s.class_id,
               c.class_name,
               s.status
        FROM student s
                 LEFT JOIN class_info c ON s.class_id = c.id
        WHERE s.id = #{id}
    </select>
</mapper>
